{
  "name": "bamazon",
  "version": "1.0.0",
  "description": "Command line application that will get data from bamazon database that will display items to the user wich they can choose if they want to buy them",
  "main": "index.js",
  "scripts": {
    "mysql-import": "mysql -h 127.0.0.1 -uroot < bamazonSchema.sql",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mysql": "^2.15.0",
    "requirer": "0.0.4"
  }
}
